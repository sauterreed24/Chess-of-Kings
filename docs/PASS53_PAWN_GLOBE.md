---
name: Pass 53 — Pawn globe catch-light
overview: "v0.5.45 wave: ivory/lapis pawn orbs grow from a 2px phone speck to a readable globe bloom with a hot spark; queen pearls scale with the same catch-light language."
---

# Pass 53 — Pawn globe catch-light (v0.5.45)

Playtest on 390×844 after Pass 52: calibration e2 `.piece-orb` boxed at **2×2 CSS pixels**. The cup was 5×4. The first board’s eight pawn heads still read as blank turned cups.

## Shipped

1. **Ivory/lapis pawns** enlarge `.piece-orb` (r 1.42 → 2.72) and add a lamp-side `.piece-spark` so the globe catch-light reads on a ~40px phone square.
2. **Queen pearls** grow with the same language (r 1.05 → 1.62).
3. **Forced Visual: Lean** keeps orb and spark. High-contrast stays uncarved.
4. **Playwright** — calibration e2 orb is at least 4×4 CSS pixels and shows `.piece-spark`. Title honor still has none (no pawns in the guard).

## Out of scope

- CSS budget changes
- Replacing Wikipedia Staunton paths
- High-contrast tournament skin

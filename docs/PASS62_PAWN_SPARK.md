---
name: Pass 62 — Pawn spark
overview: "v0.5.54 wave: ivory/lapis pawn globe sparks enlarge from a 1.8px phone speck to a readable hot core on the first-board pawns."
---

# Pass 62 — Pawn spark (v0.5.54)

Playtest on 390×844 after Pass 61: calibration e2 `.piece-spark` boxed at **1.8×1.8 CSS pixels**. Ferrules were 2.6px. The globe bloom was 4.7px. The spark still read as a pinprick inside the orb.

## Shipped

1. **Ivory/lapis pawns** enlarge the globe spark (r 1.05 → 1.55) so the hot core reads on a ~40px phone square and stays smaller than the orb.
2. **Forced Visual: Lean** keeps the geometry. High-contrast stays uncarved.
3. **Playwright** — calibration e2 spark is at least 2.4 CSS pixels across.

## Out of scope

- CSS budget changes
- Replacing Wikipedia Staunton paths
- High-contrast tournament skin

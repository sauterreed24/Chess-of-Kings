---
overview: "v0.5.130 wave: enlarge the Passage N · total crawl chip."
---

# Pass 138 — Passage chips (v0.5.130)

Playtest after Pass 136: chapter hub stamps read. Phone prose still used a 0.5rem `#scene-progress` chip — about 8.5px on the 17px root — so Passage 1 · N was the first unreadable crawl chrome on the opening scene.

## Shipped

1. **Graphics** — `renderScene` and the duel launcher set `#scene-progress` to 0.7rem. No new CSS.
2. **Playwright** — 390×844 skip-ahead prose asserts the chip is 0.7rem.

## Out of scope

- CSS budget
- Native shells
- Phone board scenes (crawl stays hidden)
